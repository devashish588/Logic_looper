# Logic Looper 🧠

A daily puzzle game with 5 rotating logic challenge types — streak tracking, achievements, and offline-first play.

## Project Structure

```
logic-looper/
├── frontend/     # React + Vite client app
│   ├── src/
│   ├── .env
│   └── package.json
├── backend/      # Express API server
│   ├── src/
│   ├── prisma/
│   ├── .env
│   └── package.json
└── .gitignore
```

## Quick Start

### Frontend
```bash
cd frontend
npm install
npm run dev        # → http://localhost:5173
```

### Backend
```bash
cd backend
npm install
npm run dev        # → http://localhost:5000
```

## Env Files
Each folder has a `.env.example` — copy to `.env` and fill in values.
