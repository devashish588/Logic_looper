{
    "name": "logic-looper-root",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "npx concurrently -n \"FRONTEND,BACKEND\" -c \"cyan,magenta\" \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
        "db-ui": "npm run db:studio --prefix backend"
    },
    "devDependencies": {
        "concurrently": "^9.2.1"
    }
}